global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: open-assistant-gateway
    metrics_path: /metrics
    static_configs:
      - targets: ["gateway:7001"]
